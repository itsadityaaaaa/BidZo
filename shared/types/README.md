# Shared Types

This directory contains TypeScript type definitions shared between the client and server.

## Usage

```typescript
import { User } from '../types/index.ts';
```

## Examples

- `api.ts` - API response/request types
- `models.ts` - Data model types
- `errors.ts` - Custom error types
